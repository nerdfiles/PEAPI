# PEAPI

Keep track of your proofofexistence.com records through a simple CLI.

## Install

    $ (sudo) npm install PE -g

## Use

    $ pe logs

      (hash) : (date) : (status)

    $ pe check <file>

      (status)

    $ pe reg <file>

      (address)
